# Postgres connection for Prisma (explorer reads, indexer writes).
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/zul?schema=public"

# ZUL node RPC for the live slot ticker.
ZUL_RPC_URL="http://127.0.0.1:8899"
